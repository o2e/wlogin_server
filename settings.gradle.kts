pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        mavenLocal()
        maven { setUrl("https://maven.aliyun.com/repository/public/") }
    }
}
rootProject.name = "wlogin_server"

